#include "QMNamespace.h"

/*!
    \namespace QM
    \brief The QM namespace contains miscellaneous identifiers used throughout the QtMediate library.
*/

namespace QM {

    /*!
        \enum ButtonState
        \brief Describes the different state of a push button.

        \var ButtonState::ButtonNormal
        \brief Normal state

        \var ButtonState::ButtonHover
        \brief Mouse over state

        \var ButtonState::ButtonPressed
        \brief Mouse down state

        \var ButtonState::ButtonDisabled
        \brief Disabled state

        \var ButtonState::ButtonNormalChecked
        \brief Normal and selected state

        \var ButtonState::ButtonHoverChecked
        \brief Mouse over and selected state

        \var ButtonState::ButtonPressedChecked
        \brief Mouse down and selected state

        \var ButtonState::ButtonDisabledChecked
        \brief Disabled and selected state
    */

}